# Sightline — API CI
# Placeholder. Workflows defined in Phase 1 (IaC/CI scaffolding).
name: API CI

on:
  push:
    paths:
      - "api/**"
  pull_request:
    paths:
      - "api/**"

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "API CI placeholder — implement in Phase 2"
