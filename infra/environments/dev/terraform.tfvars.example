# Copy to terraform.tfvars and fill in values.
# terraform.tfvars is gitignored — never commit values.

resource_group_name =
location            =

db_admin_password =
developer_ip      =

api_image =

auth0_domain        =
auth0_api_audience  =
